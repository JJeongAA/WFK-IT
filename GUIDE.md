# 🧑‍🏫 수업 가이드 — 비밀편지 만들기

기초 파이썬을 배운 학생들이 **웹 + 파일 저장** 개념을 익히는 중상 난이도 프로젝트예요.
데이터베이스를 쓰지 않고 **JSON 파일**로 저장하는 것이 핵심입니다.

---

## 1. 이 프로젝트로 배우는 것

| 개념 | 어디서 배우나 |
|------|----------------|
| Flask 라우팅 (`@app.route`) | 모든 화면 함수 |
| 폼 데이터 받기 (`request.form`) | `create`, `write`, `inbox` |
| 딕셔너리 / 리스트 다루기 | `mailboxes[name] = {...}`, `letters.append(...)` |
| **파일 입출력 + JSON** | `load_json`, `save_json` ← 가장 중요 |
| **비밀번호 해시** | `generate_password_hash` / `check_password_hash` |
| **세션(로그인 상태 기억)** | `session["auth_...]`, `app.secret_key` |
| 조건문 / 반복문 (템플릿) | `inbox.html` 의 `{% for %}` |
| 정규식 간단 검사 | `NICK_RE` (닉네임 형식) |

---

## 2. 큰 그림 (데이터가 흐르는 길)

```
[친구]  공유 링크 /box/<닉네임> 열기  →  write.html  →  POST /box/<닉네임>
             │
             ▼
   letters/<닉네임>.json 에 편지 추가 (save_json)
             │
[주인]  홈에서 닉네임+비밀번호 로그인  →  세션 저장  →  inbox.html 로 편지 확인
```

- **공유 링크** `/box/<닉네임>` : 누구나 접근 (익명 편지 쓰기)
- **로그인** `/box/<닉네임>/inbox` : 비밀번호 맞으면 세션에 기록 → 편지 보기
- 비밀번호는 **해시**로 저장돼요 (`mailboxes.json` 을 열어도 원문이 안 보임).

> 💬 **핵심 질문으로 수업 열기:** "비밀번호를 파일에 그냥 적으면 왜 위험할까?"
> → 답: 파일이 유출되면 다 털린다. 그래서 **해시**(되돌릴 수 없는 변환)로 저장한다.
> 또 하나: "로그인했다는 걸 서버가 어떻게 기억할까?" → **세션(쿠키)**.

---

## 3. 추천 진행 순서 (3~4차시)

### 1차시 — 실행부터 해보기
- 완성된 모습을 먼저 **시연**해서 무엇을 만드는지 눈으로 보여주기
  (선생님 로컬의 완성본으로 시연 — 학생 저장소엔 정답 코드가 없어요)
- 편지함 만들고 → 옆 친구에게 링크 보내 → 편지 받아보기
- `data/` 폴더 안에 **진짜 파일이 생기는 것** 확인하기 (JSON 열어보기)

### 2차시 — 파일 저장 이해 (`TODO 1`)
- `load_json`, `save_json` 를 학생이 직접 작성 (`app_student.py`)
- "왜 `encoding="utf-8"` 이 필요할까?" (한글!), "`ensure_ascii=False` 는?" 토론

### 3차시 — 편지 추가 & 삭제 (`TODO 2, 3`)
- 편지를 리스트에 `append` 하고 파일에 저장하기 (TODO 2)
- 리스트 컴프리헨션으로 특정 편지만 지우기 (TODO 3)
- 남는 시간엔 CSS 색·이모지 바꿔 나만의 디자인

> **학생이 채우는 TODO는 3개** (app_student.py): 1) 파일 읽기/쓰기 · 2) 편지 추가 · 3) 편지 삭제
> **비밀번호 해시 · 로그인 · 로그아웃은 완성 코드로 제공** — 어려워서 '읽고 이해'만 시켜요.
> 원리는 짚어주되(아래 4번 참고), 채우게 하지 않아요.

---

## 4. 자주 나오는 질문 / 막히는 곳

- **"편지가 안 보여요"** → `data/letters/<닉네임>.json` 이 생겼는지 확인. `save_json` 을 안 불렀을 확률이 큼.
- **"한글이 깨져요 (\uXXXX)"** → `json.dump(..., ensure_ascii=False)` 빠졌는지 확인.
- **"편지가 안 보여요"** → `TODO 2` 에서 `save_json` 을 불렀는지 확인 (append만 하면 안 됨).
- **"삭제가 안 돼요 / 다 지워져요"** → `TODO 3` 의 조건이 `!=` 인지 확인 (`==` 이면 반대로 동작).
- **로그인/비밀번호는 완성 제공**이라 학생이 건드릴 필요 없어요. (원리만 설명)
- **"닉네임을 못 만들어요"** → 띄어쓰기·특수문자 없이 2~20자인지 (`NICK_RE`).
- **템플릿을 못 찾음(TemplateNotFound)** → `app_student.py` 와 `templates/` 폴더가 같은 위치인지 확인.

---

## 5. 확장 과제 (더 하고 싶은 학생용)

- 🎨 **아이콘/색 커스터마이즈**: `ICONS` 목록과 `style.css` 색을 바꿔보기
- 🔢 **편지 개수 배지**, 최신순/오래된순 정렬 토글
- 🧹 **부적절한 단어 필터**: 금지어 리스트로 간단히 걸러내기
- 🗑️ **편지 삭제 버튼** (주인만): 특정 편지 지우기 라우트 추가
- ⏰ **시간 표시 개선**: "3분 전" 같은 상대 시간
- 🔒 **글자 수 제한 안내**, 빈 편지 막기(이미 되어 있음 — 코드에서 찾아보기)

---

## 6. 안전 / 개인정보 한마디 (수업 중 짚어주기)

- 익명이라도 **서버(파일)에는 편지 내용이 남아요.** "완전 비밀"은 아니라는 점.
- 실제 서비스라면 신고/차단 기능이 필요하다는 점 → 좋은 토론 주제.
- 친구를 존중하는 편지 쓰기 (익명이라고 나쁜 말 쓰지 않기) 🌷

---

## 7. 언어 (Мова / Language)

화면 글자와 코드 주석은 **우크라이나어 + 한국어**로 함께 적혀 있어요.
- 화면에서는 **우크라이나어가 크게**, 아래에 **한글이 작게** 표시돼요 (`.ko-sub`, `.ko` 클래스).
- 한 언어만 쓰고 싶다면 `templates/*.html` 에서 `<span class="ko">...</span>` 부분만 지우면 돼요.
- 코드 주석은 각 `.py` 파일에서 `UK:` / `KO:` 줄로 나뉘어 있어요. (로직은 그대로)
